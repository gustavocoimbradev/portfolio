/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['insider.blue', 'user-images.githubusercontent.com'],
  },
}

module.exports = nextConfig
